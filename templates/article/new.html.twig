{% extends 'base.html.twig' %}

{% block title %}Créer l'Article {{ article.title }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Créer un article {{ article.title }}</h1>
        
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
